import {ExtensionCodec} from '@msgpack/msgpack';

export function createExtensionCodec<CodecContextType = undefined>(): ExtensionCodec<CodecContextType> {
    // TODO:
    return {} as any as ExtensionCodec<CodecContextType>;
}